using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestShaderField : MonoBehaviour
{
    public Shader _testShader; // A simple serialized shader field

    [SerializeField]
    int _testInt; // Another simple serialized field

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
